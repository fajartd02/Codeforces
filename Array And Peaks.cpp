#include <bits/stdc++.h>
//gagal
int main(){
	int n;
	cin >> n;
	while(n--){
		int n,k;
		cin >> n >> k;
		int arr[n + 1];
		int x= 1;
		for(int i = 0 ; i < n ; i++){
			arr[i] = x;
			x += 1;
		}
		for(int i = 0 ; i < n ; i++){
			for(int j = 0 ; j <)
		}
	}
	
	return 0;
}
